<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
  <data>
          <!-- Top menu item -->
        <menuitem id="sale_menu_root"
            name="Sales Inheritance"/>

        <menuitem id="introduction_menu"
          name = "Introduction"
          parent = "sale_menu_root"
          action="action_introduction_detail"/>

    <!-- introduction text views form -->
        <record id="introduction_views_form" model="ir.ui.view">
            <field name="name">introduction.text.view.form</field>
            <field name="model">introduction.text</field>
            <field name="arch" type="xml">
              <form string="Intruduction">
                <sheet>
                        <h2>
                           <field name="intro_seq"/>
                        </h2>
                <group>        
                      <group>
                        <field name="name"/>
                        <field name="gender"/>
                        <field name="age"/>
                      </group>
                      <group>
                        <field name="contact" widget="phone"/>
                        <field name="address"/>
                        <field name="introduction_text"/>
                      </group>
                </group>  
                </sheet>
              </form>
            </field>
        </record>

        <!-- introduction text views tree -->
        <record id="introduction_views_tree" model="ir.ui.view">
            <field name="name">introduction.text.view.tree</field>
            <field name="model">introduction.text</field>
            <field name="arch" type="xml">
             <tree>
                    <field name="name"/>
                    <field name="gender"/>
                    <field name="age"/>
                    <field name="contact"/>
                    <field name="address"/>

              </tree>
            </field>
        </record> 



      <!-- search views -->
       <record id="introduction_view_search" model="ir.ui.view">
        <field name="name">introduction.text.view.search</field>
        <field name="model">introduction.text</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="gender"/>
                <field name="age"/>

                <!-- filter tage to filter record based on gender -->
                <filter name='male' string='Male' domain="[('gender','=','male')]"/>
                <separator/>

                <filter name='female' string='Female' domain="[('gender','=','female')]"/>


                <!-- group by tag to filter record using groupby gender -->
                <group expand="0" string="Group By">
                    <filter string="Gender" name="group_by_gender" context="{'group_by' : 'gender'}"/>

                </group>
                        <!-- searchpanel tag -->
                        <searchpanel>
                            <field name = "gender" icon = "fa-user-o" />
                        </searchpanel>
            </search>
          </field>
        </record>


                <!-- action field -->

      <record model="ir.actions.act_window" id="action_introduction_detail">
            <field name="name">Introduction</field>
           <field name="res_model">introduction.text</field>
            <field name="view_mode">tree,form</field>
        </record>

  </data>
</odoo>
